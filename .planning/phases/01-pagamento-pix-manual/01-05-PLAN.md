---
phase: phase-1
plan: 05
type: execute
wave: 5
depends_on: [phase-1-04]
files_modified:
  - templates/pagamentos/mock_cartao.html
  - templates/pagamentos/sucesso.html
autonomous: false
requirements:
  - REQ-01
  - REQ-02
  - REQ-03
  - REQ-04
  - REQ-05
  - REQ-06
  - REQ-07
  - REQ-08
  - REQ-09
  - REQ-10
  - REQ-11
  - REQ-12
  - REQ-13
  - REQ-14

must_haves:
  truths:
    - "Full test suite passes with 0 failures"
    - "No reference to mercadopago, Stripe, or MP_ACCESS_TOKEN anywhere in apps/"
    - "Customer can complete full PIX flow end-to-end"
    - "Restaurant can accept an order and it enters the production pipeline"
    - "Success page reassures customer that order is pending restaurant confirmation"
  artifacts:
    - path: "templates/pagamentos/sucesso.html"
      provides: "Updated success page explaining aguardando_confirmacao state"
  key_links:
    - from: "templates/pagamentos/sucesso.html"
      to: "pedido.status == aguardando_confirmacao message"
      via: "template conditional"
      pattern: "aguardando_confirmacao"
---

<objective>
Wave 4 cleanup and verification: delete the obsolete mock_cartao.html template (no card
flow exists anymore), update the success page to explain the aguardando_confirmacao state
to the customer, run the full test suite, and perform a comprehensive codebase audit for
any remaining stale MP/Stripe references.

Purpose: The flow is functionally complete after Wave 3. This wave removes dead code,
polishes the customer-facing success messaging, and gates the phase on a clean test run.

Output: Deleted mock_cartao.html, updated sucesso.html, clean test suite, stale reference
audit complete.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/phase-1/CONTEXT.md
@.planning/phases/phase-1/phase-1-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete dead templates and update sucesso.html for aguardando_confirmacao</name>
  <files>templates/pagamentos/mock_cartao.html, templates/pagamentos/sucesso.html</files>
  <action>
    Step 1 — Delete templates/pagamentos/mock_cartao.html. This file is referenced nowhere
    in the new flow (the mock_cartao_checkout view was removed in Wave 2). Delete it.

    Step 2 — Read the current templates/pagamentos/sucesso.html. The success page is shown
    after the customer uploads the comprovante. Update it to reflect the new flow: the
    customer's order is NOT yet confirmed — it's in aguardando_confirmacao state, waiting
    for the restaurant to accept.

    Update or replace the success page content to include a clear message that:
    - The comprovante was received
    - The restaurant will review and confirm the payment
    - The order will enter production after confirmation
    - Customer can track the order at `/pedidos/<id>/acompanhar/`

    The page should display pedido.status contextually:
    ```html
    {% if pedido.status == 'aguardando_confirmacao' %}
    <div class="alert alert-info">
        <i class="bi bi-hourglass-split me-2"></i>
        <strong>Comprovante recebido!</strong> O restaurante irá verificar seu pagamento.
        Assim que confirmado, seu pedido entra na fila de produção.
    </div>
    {% elif pedido.pago %}
    <div class="alert alert-success">
        <i class="bi bi-check-circle-fill me-2"></i>
        <strong>Pagamento confirmado!</strong> Seu pedido está em produção.
    </div>
    {% endif %}
    ```
    Also add a "Acompanhar meu pedido" link pointing to `{% url 'acompanhar_pedido' pedido.id %}`.

    Step 3 — Stale reference audit. Run these checks and fix any results:
    ```
    grep -r "mercadopago" apps/pagamentos/services.py apps/pagamentos/views.py apps/pagamentos/urls.py config/ templates/ requirements.txt
    grep -r "stripe_payment_intent_id" apps/ config/ templates/
    grep -r "MP_ACCESS_TOKEN\|MP_PIX_PAYER_EMAIL\|PAYMENT_GATEWAY" apps/ config/ templates/
    grep -r "pagamento_escolher\|iniciar_pagamento_mp\|mp_webhook\|mp_checkout_return\|mock_cartao" apps/ config/ templates/
    ```
    Each grep must return zero matches. Keep only the legacy enum option in
    `apps/pagamentos/models.py`:
    `('mercadopago', 'Mercado Pago (descontinuado)')`.
  </action>
  <verify>
    <automated>python manage.py test apps.pagamentos.tests.test_views apps.pagamentos.tests.test_services apps.pedidos.tests.test_status --verbosity=1</automated>
    <automated>python manage.py test --verbosity=1 2>&amp;1 | tail -10</automated>
    Targeted smoke suite must pass first, then full test suite must pass with 0 failures and 0 errors.
    Also: `python manage.py check` must show 0 errors.
  </verify>
  <done>
    - templates/pagamentos/mock_cartao.html deleted
    - sucesso.html explains aguardando_confirmacao state to customer
    - sucesso.html has link to acompanhar_pedido
    - All stale MP/Stripe grep checks return zero matches (except legacy enum in `apps/pagamentos/models.py`)
    - Full test suite passes
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human end-to-end validation of PIX manual flow</name>
  <action>
    Execute the manual end-to-end checklist below in a running local environment.
    Do not edit code in this task; capture pass/fail evidence and unblock only
    when all blocking checks are green.
  </action>
  <what-built>
    Complete PIX manual payment flow:
    - Customer PIX page with fixed key + copy button
    - Proof upload with file type + size validation
    - Order transitions to aguardando_confirmacao after upload (pago=False)
    - Restaurant painel shows pending PIX orders with warning badge
    - Restaurant can view uploaded comprovante and accept or reject
    - Accept: pago=True, status=recebido (enters production pipeline)
    - Reject: status=cancelado
    - Dashboard totals count accepted PIX orders (pago=True gate preserved)
    - Mercado Pago fully removed
    - All 14 acceptance criteria implemented
  </what-built>
  <how-to-verify>
    1. Start the server: `python manage.py runserver`

    2. Customer flow:
       a. Add items to cart, proceed to checkout, place an order
       b. You should be redirected to `/pagamentos/<id>/` — verify PIX key is shown
       c. Click the copy button — verify "Copiado!" appears
       d. Click "Já paguei — Enviar Comprovante" — verify upload form appears
       e. Upload any image file (jpg or png) — verify redirect to success page
       f. Verify success page says comprovante received, awaiting confirmation

    3. Restaurant flow:
       a. Log in to the painel at `/painel/`
       b. Verify navigation badge shows the pending PIX order count
       c. In Pedidos list, verify the pending PIX order appears in the yellow section
       d. Click "Revisar" — verify the detalhe page shows accept/reject buttons
       e. Click "Visualizar Comprovante" — verify the uploaded file opens
       f. Click "Aceitar Pagamento" — verify redirect and order shows status "Recebido"
       g. Verify the accepted order now appears in the main (pago=True) orders list
       h. Test with a second order: go to accept/reject → click "Rejeitar" → verify order cancelled

    4. Admin check:
       a. Visit `/admin/pedidos/pedido/<id>/change/` — verify no FieldError
       b. Visit `/admin/pagamentos/pagamento/<id>/change/` — verify comprovante field visible

    5. Verify Mercado Pago is gone:
       a. Confirm `/pagamentos/<id>/` no longer shows card/PIX option selector
       b. Confirm there are no 500 errors anywhere in the flow
  </how-to-verify>
  <verify>
    <automated>python manage.py test apps.pagamentos.tests.test_views apps.pagamentos.tests.test_services apps.pedidos.tests.test_status --verbosity=1</automated>
    Smoke regression for PIX manual flow must pass before human approval.
  </verify>
  <resume-signal>Type "approved" if the full flow works correctly, or describe any issues found.</resume-signal>
</task>

</tasks>

<verification>
Phase gate checks (all must pass before marking phase complete):
1. `python manage.py test --verbosity=1` — full suite green
2. `python manage.py check` — 0 errors
3. `grep -r "mercadopago" apps/pagamentos/services.py apps/pagamentos/views.py apps/pagamentos/urls.py requirements.txt` — 0 results (legacy enum in `apps/pagamentos/models.py` is allowed)
4. `grep -r "stripe_payment_intent_id" apps/` — 0 results
5. Customer can upload comprovante → pedido.status=aguardando_confirmacao, pago=False
6. Restaurant accept → pago=True, status=recebido
7. Restaurant reject → status=cancelado
8. Django Admin Pedido change form opens without FieldError
</verification>

<success_criteria>
Phase 1 complete when:
- All 14 acceptance criteria verified by human review (checkpoint above)
- Full test suite passes with 0 failures
- No Mercado Pago or Stripe references anywhere
- Customer flow: PIX key → copy → upload comprovante → success page
- Restaurant flow: see pending orders → view comprovante → accept or reject
- Production pipeline preserved after acceptance (existing status machine unchanged except new node)
- PIX_KEY configurable via env var (AC-12)
- File upload: max 10MB, types jpg/jpeg/png/webp/pdf (AC-13)
</success_criteria>

<output>
After completion, create `.planning/phases/phase-1/phase-1-05-SUMMARY.md` with:
- Final test count and results
- Stale reference audit results
- Any issues found during human verification and how they were fixed
- Phase 1 declared complete
</output>

