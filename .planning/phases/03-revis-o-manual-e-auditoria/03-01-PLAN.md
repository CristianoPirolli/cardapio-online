---
phase: 03-revis-o-manual-e-auditoria
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/pagamentos/models.py
  - apps/pagamentos/migrations/0009_pagamento_revisao_historico.py
  - apps/pagamentos/forms.py
  - apps/pagamentos/views.py
  - templates/painel/pedido_detalhe.html
  - apps/pagamentos/tests/test_revisao_manual.py
  - apps/pagamentos/tests/test_auditoria_revisao.py
autonomous: true
requirements:
  - OPS-02
  - OPS-03
must_haves:
  truths:
    - "Aprovar/rejeitar pagamento PIX exige motivo e justificativa valida (minimo 10 caracteres)."
    - "Decisao manual registra metadados operacionais com operador e data/hora no momento da aprovacao/rejeicao."
    - "Historico exibido no detalhe do pedido permanece minimo: apenas acao e data/hora (per D-09 e D-10)."
    - "Decisao sem dados obrigatorios nao altera status do pedido."
  artifacts:
    - path: "apps/pagamentos/models.py"
      provides: "Modelo de historico de revisao manual por pedido"
    - path: "apps/pagamentos/forms.py"
      provides: "Contrato de validacao de motivo + justificativa"
    - path: "apps/pagamentos/views.py"
      provides: "Endpoints aceitar/rejeitar com validacao obrigatoria e registro de auditoria"
    - path: "apps/pagamentos/tests/test_revisao_manual.py"
      provides: "Cobertura de fluxo de aprovacao/rejeicao com justificativa obrigatoria"
  key_links:
    - from: "templates/painel/pedido_detalhe.html"
      to: "apps/pagamentos/views.py::aceitar_pix/rejeitar_pix"
      via: "POST com motivo + justificativa"
      pattern: "motivo_revisao|justificativa_revisao"
    - from: "apps/pagamentos/views.py"
      to: "apps/pagamentos/models.py::PagamentoRevisaoHistorico"
      via: "registro append-only por decisao manual"
      pattern: "PagamentoRevisaoHistorico.objects.create"
---

<objective>
Estabelecer o contrato de revisao manual com justificativa obrigatoria e persistencia auditavel por pedido.

Purpose: Cobrir OPS-02 e a base de OPS-03 sem alterar o fluxo manual de comprovante.
Output: modelo/form/views/templates/testes para decisao manual auditavel.
</objective>

<execution_context>
@C:/Users/crist/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/crist/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-revis-o-manual-e-auditoria/03-CONTEXT.md
@.planning/phases/03-revis-o-manual-e-auditoria/03-RESEARCH.md
@.planning/phases/03-revis-o-manual-e-auditoria/03-UI-SPEC.md
@apps/pagamentos/views.py
@templates/painel/pedido_detalhe.html
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Criar testes de contrato para revisao manual e auditoria</name>
  <files>apps/pagamentos/tests/test_revisao_manual.py, apps/pagamentos/tests/test_auditoria_revisao.py</files>
  <read_first>
@apps/pagamentos/views.py
@apps/pagamentos/models.py
@templates/painel/pedido_detalhe.html
@.planning/phases/03-revis-o-manual-e-auditoria/03-CONTEXT.md
  </read_first>
  <action>
    Criar testes RED cobrindo:
    1) aceitar/rejeitar sem motivo ou sem justificativa valida retorna erro e nao altera pedido;
    2) justificativa com menos de 10 caracteres e rejeitada;
    3) motivo aceito apenas entre valido/invalido/outro;
    4) decisao valida registra operador + data/hora como metadado da decisao (criterio do ROADMAP #2);
    5) feed exibido no detalhe permanece minimo (acao + data/hora), sem exposicao de operador/motivo/justificativa (D-09/D-10).
    Incluir cenarios de isolamento por restaurante (usuario nao revisa pedido de outro tenant).
  </action>
  <verify>
    <automated>venv\Scripts\python manage.py test apps.pagamentos.tests.test_revisao_manual apps.pagamentos.tests.test_auditoria_revisao --verbosity=1 --keepdb --noinput</automated>
  </verify>
  <acceptance_criteria>
    - Arquivos de teste existem e executam via `manage.py test`.
    - Suite falha inicialmente pelos comportamentos ainda nao implementados (RED explicito).
  </acceptance_criteria>
  <done>Contratos de comportamento para OPS-02/OPS-03 estao formalizados em testes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implementar modelo e formulario de revisao obrigatoria</name>
  <files>apps/pagamentos/models.py, apps/pagamentos/migrations/0009_pagamento_revisao_historico.py, apps/pagamentos/forms.py, apps/pagamentos/tests/test_auditoria_revisao.py</files>
  <read_first>
@apps/pagamentos/models.py
@apps/pagamentos/forms.py
@apps/pagamentos/migrations/0008_chave_pix_models_and_snapshot.py
@.planning/phases/03-revis-o-manual-e-auditoria/03-RESEARCH.md
  </read_first>
  <action>
    Criar modelo dedicado de auditoria de revisao manual por pedido (append-only) com campos obrigatorios de decisao:
    pedido, pagamento (nullable), acao, motivo, justificativa, operador e criado_em.
    Implementar validacao server-side em form de revisao (`motivo` + `justificativa` com minimo de 10 chars), usando choices locked `valido`, `invalido`, `outro` (D-08) e exigencia D-05/D-06/D-07.
    Gerar migracao e garantir indices/ordenacao por recencia para leitura no detalhe.
    Reconciliar explicitamente criterio do ROADMAP #2 com D-09: operador deve ser persistido como metadado de decisao, mas nao precisa aparecer no feed exibido no detalhe.
  </action>
  <verify>
    <automated>venv\Scripts\python manage.py test apps.pagamentos.tests.test_auditoria_revisao --verbosity=1 --keepdb --noinput</automated>
  </verify>
  <acceptance_criteria>
    - Modelo append-only criado e migracao aplicada sem conflito.
    - Formulario rejeita entradas invalidas e aceita motivo/justificativa validos.
    - Registro de decisao persiste operador + timestamp para aprovar/rejeitar.
    - Contrato de exibicao minima (acao + data/hora) permanece preservado para a UI de historico.
  </acceptance_criteria>
  <done>Base de persistencia e validacao obrigatoria da revisao manual implementadas.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Integrar aceitar/rejeitar com justificativa obrigatoria e log de auditoria</name>
  <files>apps/pagamentos/views.py, templates/painel/pedido_detalhe.html, apps/pagamentos/tests/test_revisao_manual.py</files>
  <read_first>
@apps/pagamentos/views.py
@apps/pagamentos/services.py
@templates/painel/pedido_detalhe.html
@.planning/phases/03-revis-o-manual-e-auditoria/03-UI-SPEC.md
  </read_first>
  <action>
    Atualizar `aceitar_pix` e `rejeitar_pix` para processar o form de revisao em transacao atomica:
    validar payload, executar service existente (confirmar/rejeitar), registrar evento de auditoria com operador (`request.user`) + timestamp e responder via mensagens Django.
    Atualizar `pedido_detalhe.html` para substituir botoes diretos por formulario de decisao com dropdown de motivo + textarea de justificativa.
    Garantir que o bloco de historico mostrado no detalhe exiba apenas acao + data/hora (D-09), mesmo com metadados adicionais persistidos.
    Manter confirmacao explicita na rejeicao e preservar fluxo manual sem gateway/webhook (fora de escopo).
  </action>
  <verify>
    <automated>venv\Scripts\python manage.py test apps.pagamentos.tests.test_revisao_manual apps.pagamentos.tests.test_auditoria_revisao --verbosity=1 --keepdb --noinput</automated>
  </verify>
  <acceptance_criteria>
    - Aprovar/rejeitar sem motivo/justificativa nao executa decisao.
    - Aprovar/rejeitar com dados validos executa decisao e grava evento auditavel com operador + timestamp.
    - Mensagens de erro/sucesso seguem contrato do UI-SPEC.
    - Historico exibido ao operador continua minimo (acao + data/hora), conforme D-09/D-10.
  </acceptance_criteria>
  <done>Fluxo de decisao manual fica bloqueado por justificativa obrigatoria e auditado por pedido.</done>
</task>

</tasks>

<verification>
1. `venv\Scripts\python manage.py test apps.pagamentos.tests.test_revisao_manual apps.pagamentos.tests.test_auditoria_revisao --verbosity=1 --keepdb --noinput`
2. `venv\Scripts\python manage.py check`
</verification>

<success_criteria>
OPS-02 operacional: decisao manual exige justificativa valida.
OPS-03 base operacional: cada decisao gera trilha auditavel persistida por pedido.
</success_criteria>

<output>
After completion, create `.planning/phases/03-revis-o-manual-e-auditoria/03-01-SUMMARY.md`
</output>
