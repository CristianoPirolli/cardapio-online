---
phase: 02-gest-o-de-chaves-pix
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - apps/pagamentos/forms.py
  - apps/pagamentos/views.py
  - apps/pagamentos/urls.py
  - apps/restaurantes/urls.py
  - templates/painel/base_painel.html
  - templates/painel/pix_keys.html
  - apps/pagamentos/tests/test_views_chaves_pix.py
autonomous: true
requirements:
  - PAY-15
  - PAY-16
must_haves:
  truths:
    - "Restaurante consegue cadastrar, editar, ativar/desativar e ordenar multiplas chaves PIX no painel."
    - "Somente uma chave padrao ativa e uma prioridade ativa por restaurante sao aceitas."
    - "Tela do painel mostra historico de mutacoes com quem/quando/acao/antes/depois."
  artifacts:
    - path: "apps/pagamentos/forms.py"
      provides: "Form de cadastro/edicao com validacao por tipo"
    - path: "templates/painel/pix_keys.html"
      provides: "Tela unica de gestao completa com CRUD + historico"
    - path: "apps/pagamentos/views.py"
      provides: "Views de painel para mutacoes de chave e log historico"
    - path: "apps/pagamentos/tests/test_views_chaves_pix.py"
      provides: "Cobertura de fluxo de painel e regras de integridade"
  key_links:
    - from: "templates/painel/pix_keys.html"
      to: "apps/pagamentos/views.py"
      via: "form actions de criar/editar/ativar/desativar/padrao/prioridade"
      pattern: "url 'painel_pix_keys"
    - from: "apps/pagamentos/views.py"
      to: "apps/pagamentos/models.py::ChavePixHistorico"
      via: "registro append-only de mutacoes"
      pattern: "ChavePixHistorico.objects.create"
    - from: "templates/painel/base_painel.html"
      to: "apps/restaurantes/urls.py"
      via: "item de navegacao para tela de chaves PIX"
      pattern: "painel_pix_keys"
---

<objective>
Entregar gestao completa de chaves PIX no painel em tela unica, com trilha auditavel de mutacoes e sem alterar o fluxo manual de confirmacao.

Purpose: Atender PAY-15 e consolidar PAY-16 no fluxo operacional do restaurante.
Output: pagina de painel + rotas + forms + testes de integracao.
</objective>

<read_first>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-gest-o-de-chaves-pix/02-CONTEXT.md
@.planning/phases/02-gest-o-de-chaves-pix/02-RESEARCH.md
@.planning/phases/02-gest-o-de-chaves-pix/02-01-PLAN.md
@apps/restaurantes/urls.py
@apps/pagamentos/views.py
@templates/painel/base_painel.html
@templates/painel/pedidos.html
</read_first>

<execution_context>
@C:/Users/crist/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/crist/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/01-pagamento-pix-manual/01-04-SUMMARY.md
@.planning/phases/01-pagamento-pix-manual/01-05-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Criar fluxo de painel para CRUD completo de chaves PIX em tela unica</name>
  <files>apps/pagamentos/forms.py, apps/pagamentos/views.py, apps/pagamentos/urls.py, apps/restaurantes/urls.py, templates/painel/pix_keys.html</files>
  <behavior>
    - Teste 1: restaurante cria multiplas chaves e edita valores/tipo sem sair da tela (PAY-15, per D-01).
    - Teste 2: ativar/desativar e definir padrao respeitam regras de integridade (PAY-16, per D-01).
    - Teste 3: prioridade ativa duplicada por restaurante e rejeitada (locked default).
    - Teste 4: validacao por tipo rejeita CPF/CNPJ/email/telefone/UUID invalidos (per D-02).
  </behavior>
  <action>
    Implementar em `apps/pagamentos/views.py` as acoes de painel (listar, criar, editar, ativar/desativar, definir padrao e prioridade) restritas ao restaurante do usuario logado.
    Criar `apps/pagamentos/forms.py` para validacoes de tipo e mensagens operacionais objetivas.
    Expor rota principal no namespace do painel (via `apps/restaurantes/urls.py`) e endpoints POST auxiliares em `apps/pagamentos/urls.py`.
    Construir `templates/painel/pix_keys.html` com dois blocos na mesma tela: tabela/formulario de gestao e trilha de historico (per D-01).
    Nao incluir qualquer webhook/gateway/reembolso/split (D-04 e ideias deferidas).
  </action>
  <verify>
    <automated>venv\Scripts\python manage.py test apps.pagamentos.tests.test_views_chaves_pix --verbosity=1 --keepdb --noinput</automated>
  </verify>
  <done>Tela unica de painel cobre CRUD operacional completo com regras de padrao/prioridade e validacao de tipo.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Registrar historico de mutacoes no painel com antes/depois</name>
  <files>apps/pagamentos/views.py, templates/painel/pix_keys.html, apps/pagamentos/tests/test_views_chaves_pix.py</files>
  <behavior>
    - Teste 1: cada mutacao de chave gera historico com ator, timestamp, acao e diff antes/depois (per D-03).
    - Teste 2: eventos de selecao no checkout nao entram nesse historico (locked default de escopo).
    - Teste 3: historico aparece na mesma tela de gestao ordenado do mais recente para o mais antigo.
  </behavior>
  <action>
    Em todas as mutacoes de painel, persistir registro append-only em `ChavePixHistorico` com payload antes/depois estruturado.
    Atualizar a tela `pix_keys.html` para exibir a trilha com colunas minimas: quem, quando, acao e diff.
    Garantir que somente mutacoes feitas no painel entram nesse historico (snapshot por pedido fica no Pagamento, nao nesse feed).
  </action>
  <verify>
    <automated>venv\Scripts\python manage.py test apps.pagamentos.tests.test_views_chaves_pix --verbosity=1 --keepdb --noinput</automated>
  </verify>
  <done>Historico operacional auditavel disponivel na mesma tela, com escopo de mutacao coerente com as decisoes.</done>
</task>

<task type="auto">
  <name>Task 3: Integrar navegacao do painel e validar regressao funcional</name>
  <files>templates/painel/base_painel.html, apps/pagamentos/tests/test_views_chaves_pix.py</files>
  <action>
    Adicionar entrada "Chaves PIX" na navegacao do painel em `base_painel.html`, com estado ativo quando estiver na tela.
    Ajustar testes para cobrir acesso autorizado/nao autorizado e regressao basica dos fluxos de pedidos existentes.
    Executar suite focal de pagamentos e depois suite completa para garantir que a fase nao quebrou o fluxo manual anterior.
  </action>
  <verify>
    <automated>venv\Scripts\python manage.py test apps.pagamentos.tests --verbosity=1 --keepdb --noinput &amp;&amp; venv\Scripts\python manage.py test --verbosity=1 --keepdb --noinput</automated>
  </verify>
  <done>Painel expoe a nova tela de chaves PIX sem regressao do fluxo de pagamento manual existente.</done>
</task>

</tasks>

<verification>
1. `venv\Scripts\python manage.py test apps.pagamentos.tests.test_views_chaves_pix --verbosity=1 --keepdb --noinput`
2. `venv\Scripts\python manage.py test apps.pagamentos.tests --verbosity=1 --keepdb --noinput`
3. `venv\Scripts\python manage.py test --verbosity=1 --keepdb --noinput`
</verification>

<acceptance_criteria>
- PAY-15: restaurante gerencia multiplas chaves PIX ativas pelo painel.
- PAY-16: restaurante define padrao/prioridade com integridade e sem ambiguidade.
- D-03: trilha mostra quem/quando/acao/antes/depois na mesma tela.
</acceptance_criteria>

<success_criteria>
Fluxo operacional de chaves PIX fica autossuficiente no painel e pronto para ser usado pelo checkout da fase 02.
</success_criteria>

<output>
After completion, create `.planning/phases/02-gest-o-de-chaves-pix/02-02-SUMMARY.md`
</output>
