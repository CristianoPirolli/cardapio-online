---
phase: 02-gest-o-de-chaves-pix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/pagamentos/models.py
  - apps/pagamentos/services.py
  - apps/pagamentos/views.py
  - templates/pagamentos/pagamento.html
  - apps/pagamentos/tests/test_chaves_pix.py
  - apps/pagamentos/tests/test_views_chaves_pix.py
  - apps/pagamentos/migrations/0008_chave_pix_models_and_snapshot.py
autonomous: true
requirements:
  - PAY-16
  - PAY-17
  - PAY-18
must_haves:
  truths:
    - "Checkout PIX sempre exibe uma chave do proprio restaurante usando regra deterministica."
    - "Mudancas de ativacao/desativacao ou prioridade nao alteram a chave de pedidos ja iniciados."
    - "Existe apenas uma chave padrao ativa por restaurante e prioridades ativas nao conflitam."
  artifacts:
    - path: "apps/pagamentos/models.py"
      provides: "Modelos ChavePix e ChavePixHistorico + campos snapshot no Pagamento"
    - path: "apps/pagamentos/services.py"
      provides: "Selecao deterministica de chave e snapshot no pagamento"
    - path: "apps/pagamentos/views.py"
      provides: "Checkout usando service de selecao sem dependencia de settings.PIX_KEY"
    - path: "apps/pagamentos/migrations/0008_chave_pix_models_and_snapshot.py"
      provides: "Schema e constraints para padrao/prioridade/snapshot"
  key_links:
    - from: "apps/pagamentos/views.py"
      to: "apps/pagamentos/services.py"
      via: "pagamento_pix_manual -> selecionar_chave_pix_checkout"
      pattern: "selecionar_chave_pix_checkout"
    - from: "apps/pagamentos/services.py"
      to: "apps/pagamentos/models.py"
      via: "persistencia do snapshot da chave no Pagamento"
      pattern: "dados_resposta.*pix_key"
    - from: "apps/pagamentos/models.py"
      to: "database constraints"
      via: "UniqueConstraint condicional por restaurante"
      pattern: "UniqueConstraint"
---

<objective>
Criar a base transacional de multi-chave PIX por restaurante e mover a selecao do checkout para regra deterministica com snapshot por pedido.

Purpose: Atender PAY-16, PAY-17 e PAY-18 sem regressao do fluxo manual existente.
Output: modelos + migracao + servico + checkout atualizados e testados.
</objective>

<read_first>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-gest-o-de-chaves-pix/02-CONTEXT.md
@.planning/phases/02-gest-o-de-chaves-pix/02-RESEARCH.md
@apps/pagamentos/models.py
@apps/pagamentos/services.py
@apps/pagamentos/views.py
@templates/pagamentos/pagamento.html
</read_first>

<execution_context>
@C:/Users/crist/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/crist/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/01-pagamento-pix-manual/01-03-SUMMARY.md
@.planning/phases/01-pagamento-pix-manual/01-04-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Criar contratos de dados e constraints de chave PIX por restaurante</name>
  <files>apps/pagamentos/models.py, apps/pagamentos/migrations/0008_chave_pix_models_and_snapshot.py, apps/pagamentos/tests/test_chaves_pix.py</files>
  <behavior>
    - Teste 1: restaurante pode ter multiplas chaves ativas validas (PAY-15 base para PAY-16).
    - Teste 2: apenas uma chave padrao ativa por restaurante (PAY-16, per D-01).
    - Teste 3: prioridade ativa deve ser unica por restaurante (locked default do planejamento).
    - Teste 4: validacao obrigatoria por tipo CPF/CNPJ/email/telefone/UUID (per D-02).
  </behavior>
  <action>
    Escrever primeiro os testes em `test_chaves_pix.py` (RED), depois implementar `ChavePix` e `ChavePixHistorico` em `apps/pagamentos/models.py`.
    Incluir constraints de banco com escopo por restaurante: padrao ativa unica e prioridade ativa unica (locked default), mantendo possibilidade de chaves inativas.
    Adicionar campos de snapshot no `Pagamento` (id/tipo/valor da chave escolhida) via campos dedicados ou JSON estruturado em `dados_resposta`, mantendo compatibilidade.
    Gerar migracao `0008_chave_pix_models_and_snapshot.py`.
    Registrar no model que historico e append-only para mutacoes de painel (per D-03); nao registrar eventos de leitura.
  </action>
  <verify>
    <automated>venv\Scripts\python manage.py test apps.pagamentos.tests.test_chaves_pix --verbosity=1 --keepdb --noinput</automated>
  </verify>
  <done>Modelos/migracao aplicam integridade de padrao/prioridade por restaurante e validacoes de tipo passam nos testes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implementar service de selecao deterministica e snapshot de consistencia</name>
  <files>apps/pagamentos/services.py, apps/pagamentos/tests/test_chaves_pix.py</files>
  <behavior>
    - Teste 1: selecao usa padrao ativa; fallback por menor prioridade e desempate por id (PAY-17).
    - Teste 2: quando nao ha chave ativa, checkout falha de forma controlada sem fallback global (per D-04).
    - Teste 3: pagamento pendente preserva snapshot mesmo apos alteracao/desativacao da chave ativa (PAY-18, per D-05).
  </behavior>
  <action>
    Implementar `selecionar_chave_pix_checkout(restaurante)` em `services.py` com ordenacao deterministica e sem usar `settings.PIX_KEY`.
    Ajustar `criar_pagamento_pix_manual` para persistir snapshot da chave escolhida no momento de criacao/reuso de pagamento.
    Usar `transaction.atomic()` e lock apropriado nas rotinas que alterarem padrao/prioridade para evitar corridas.
    Manter fluxo sem gateway/webhook e sem alteracoes no processo de confirmacao manual (per D-04).
  </action>
  <verify>
    <automated>venv\Scripts\python manage.py test apps.pagamentos.tests.test_chaves_pix --verbosity=1 --keepdb --noinput</automated>
  </verify>
  <done>Service seleciona chave de forma deterministica e pedidos em andamento preservam chave snapshot.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Integrar checkout para exibir chave selecionada do restaurante</name>
  <files>apps/pagamentos/views.py, templates/pagamentos/pagamento.html, apps/pagamentos/tests/test_views_chaves_pix.py</files>
  <behavior>
    - Teste 1: checkout exibe chave do restaurante conforme regra configurada (PAY-17).
    - Teste 2: checkout nao usa mais `settings.PIX_KEY` quando houver configuracao por restaurante.
    - Teste 3: pagamento reapresentado usa snapshot existente do pedido em andamento (PAY-18).
  </behavior>
  <action>
    Atualizar `pagamento_pix_manual` para carregar chave via service e renderizar metadados (tipo/chave mascarada quando aplicavel) no template.
    Manter layout funcional atual e mensagens claras quando restaurante nao possuir chave ativa.
    Garantir que o checkout nao introduza qualquer integracao de gateway/webhook (per D-04).
  </action>
  <verify>
    <automated>venv\Scripts\python manage.py test apps.pagamentos.tests.test_chaves_pix apps.pagamentos.tests.test_views_chaves_pix --verbosity=1 --keepdb --noinput</automated>
  </verify>
  <done>Checkout passa a depender apenas de chave por restaurante com comportamento deterministico e cobertura de regressao.</done>
</task>

</tasks>

<verification>
1. `venv\Scripts\python manage.py test apps.pagamentos.tests.test_chaves_pix apps.pagamentos.tests.test_views_chaves_pix --verbosity=1 --keepdb --noinput`
2. `venv\Scripts\python manage.py check`
</verification>

<acceptance_criteria>
- PAY-16: regra de padrao/prioridade ativa com integridade por restaurante.
- PAY-17: chave correta exibida no checkout segundo regra deterministica.
- PAY-18: pedido iniciado preserva snapshot da chave apesar de manutencoes futuras.
</acceptance_criteria>

<success_criteria>
Base de selecao/snapshot pronta e estavel para permitir gestao completa no painel sem quebrar pedidos em andamento.
</success_criteria>

<output>
After completion, create `.planning/phases/02-gest-o-de-chaves-pix/02-01-SUMMARY.md`
</output>
